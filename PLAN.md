# Tigerest Theater / 大河影院：2.0.8 实施与验收计划

## 产品边界

本项目是面向 Windows 10/11 与 macOS 15+ 的非官方 Emby 桌面客户端。Emby Server Web UI 提供媒体库界面，桌面壳、内嵌 libmpv、UOSC、shader、下载和离线观看由本项目提供。

测试账号只允许从运行时会话或操作系统存储读取；源码、配置模板、测试快照、日志和构建产物不得包含用户名、密码或访问令牌。

## 已完成范围

- [x] 移除旧的关闭、安全、普通动画等档位，只保留“默认 / 真人 / 激进测试”。
- [x] 默认档完整采用参考 `anime-aa-insane` 参数；真人沿用真人高画质链；激进测试保留 Anime4K 可观测链。
- [x] Alt+1 / Alt+2 / Alt+3 与 UOSC 菜单在同一 libmpv 实例内切换，分别精确加载 3 / 2 / 4 个非空 shader。
- [x] 内置 `mpv.conf` 每个配置项均有中文说明；高级用户修改写入升级不会覆盖的 `user-overrides.conf`。
- [x] 字幕默认值同步参考配置，并迁移旧版遗留的 `sans-serif` 默认字体为 Segoe UI。
- [x] Windows 内置模式在 libmpv 初始化前启用 `input-ipc-server=mpvpipe`，SVP JSON IPC 与客户端 libmpv 控制、Emby 进度回传可并存。
- [x] 弹幕开关写入历史文件并在下次 `file-loaded` 时恢复；关闭播放器时为开启，下次打开仍开启。
- [x] 保持 `gpu-next`、UOSC、完整 Stats 帧流水线、F11 全屏和单窗口播放退出修复。
- [x] 修正安装器大图标居中，并以 Tigerest 启动画面替换连接阶段短暂出现的 Jellyfin 图标。
- [x] 将嵌入式原生 mpv 窗口的鼠标、滚轮和键盘事件转发给 libmpv，恢复 UOSC 完整交互。
- [x] 为桌面鼠标增加剧集横向拖动手势，并保留短按选集行为。
- [x] 侦测 mpv 静默回退到空音频输出的状态，暂停播放并显示完整的音频设备恢复菜单。
- [x] 增补 libmpv 冒号形式认证请求头脱敏，并在启动时原地清洗旧版轮转日志。
- [x] Windows 实际编译、测试、打包、静默覆盖安装和安装目录启动完成。
- [x] 测试账号累计完成登录、浏览、在线播放、字幕、跳转、下载和下载回放验收。

## 质量门槛与停止条件

- [x] CTest 5/5 通过；静态完整性检查 9 项通过。
- [x] Windows 新构建与安装版核心流程通过；安装目录主程序与打包输入哈希一致。
- [x] 运行时验证默认 / 真人 / 激进测试为 3 / 2 / 4 个 shader，激进档出现 Anime4K Restore、Upscale、Thin。
- [x] 运行时字幕参数、跳转、播放退出、弹幕记忆和 `mpvpipe` 均通过。
- [x] 源码与配置无硬编码凭据；11 份现存日志扫描结果为未遮盖认证字段 0。
- [x] macOS 无 runner 时仅交付可复现工程，不声称实机构建或播放验证。

当前状态：可验证停止条件已满足。Windows 已确认范围无阻塞；macOS 唯一限制是当前没有 runner，因此未做实机验收。
