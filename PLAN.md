# Tigerest Theater / 大河影院：2.0.11 实施与验收计划

## 产品边界

本项目是面向 Windows 10/11 与 macOS 26+ 的非官方 Emby 桌面客户端。Emby Server Web UI 提供媒体库界面，桌面壳、内嵌 libmpv、UOSC、shader、下载和离线观看由本项目提供。

测试账号只允许从运行时会话或操作系统凭据存储读取；源码、配置模板、测试快照、日志、构建产物和 Git 历史不得包含用户名、密码或访问令牌。

## 2.0.9 已完成范围

- [x] 左键双击直接执行一次 `cycle pause`，不改变右键、滚轮和 UOSC 交互。
- [x] 单媒体加载使用原子 `loadfile replace`，队列保留 `append-play`，并返回真实命令结果。
- [x] 播放器信号使用具名处理器统一连接和断开，所有终止路径执行一次幂等清理。
- [x] 增加 30 秒启动超时；结束时关闭加载动画、移除容器、恢复页面和窗口状态。
- [x] GPU-Next 下 WebEngine 在原生视频窗口下持续合成；libmpv Render API 保留同场景隐藏兼容路径。
- [x] 全屏统一由 `WindowManager` 管理，播放结束恢复窗口化/最大化状态，预先全屏则保持全屏。
- [x] Emby 标题栏新增窗口调整按钮，UOSC 新增退出播放按钮。
- [x] 新增播放器生命周期、窗口状态、资源完整性和凭据安全回归测试。
- [x] Windows x64 安装器和便携 ZIP 使用同一 `2.0.9-dev` 主程序构建。

## 2.0.11 macOS 已完成范围

- [x] macOS 26+ 固定采用 libmpv Render API 单窗口合成，禁用不稳定的 `wid`/Cocoa 原生播放器窗口。
- [x] 保留 Windows 2.0.11 的 `stop`/`quit` 生命周期修复；UOSC 退出不销毁共享 libmpv 内核。
- [x] 真实媒体播放期间验证 mpv 全屏请求只控制 Tigerest 主窗口，进入/退出均可恢复。
- [x] 验证停止播放后应用保持运行、可再次播放，`Command+Q` 最终以退出码 0 正常结束。
- [x] 启动阶段使用深色 Tigerest 品牌遮罩覆盖 WebEngine 首帧空档。
- [x] macOS Release 构建、CTest 7/7、ad-hoc 签名、DMG 生成和镜像校验通过。

## 质量门槛

- [x] Windows 完整构建成功；CTest 7/7 通过。
- [x] 安装版和便携版生成成功；便携 ZIP 在全新目录解压并通过主程序哈希一致性检查。
- [x] 当前源码、报告和全部 3 个历史提交未发现测试账号、密码、令牌或 API 密钥字面量。
- [x] macOS 27 Apple Silicon 实机构建与播放验证通过；发布最低目标为 macOS 26.0。
- [ ] Windows 图形点击自动化控制内核本轮不可用；未声称完成连续 20 次人工双击及两种后端肉眼画面验收。

当前状态：macOS 2.0.11-dev 已满足本机发布门槛；Windows 历史实测边界继续在测试报告中明确记录。
